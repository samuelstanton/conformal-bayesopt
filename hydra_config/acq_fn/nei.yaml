obj:
  _target_: botorch.acquisition.monte_carlo.qNoisyExpectedImprovement
  prune_baseline: true
  sampler:
    _target_: botorch.sampling.samplers.IIDNormalSampler
    num_samples: ${num_samples}
    batch_range: [0, -2]
params:
  - X_baseline